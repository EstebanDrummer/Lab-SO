#include <stdio.h>

struct cdsMusica{
	char titulo[40];
	char artista[40];
	char genero[15];
	int numCanciones;
	int lanzamiento;
	int precio;
}; //cd1,cd2, cd3;
struct cdsMusica cd1={
"Brindo con el alma",
"Diomedes Diaz",
"Vallenato",
11,
1986,
19900};

int main() {
printf( "El titulo es: %s \n ", cd1.titulo );
printf( "El artista es: %s \n ", cd1.artista );
printf( "El genero es: %s \n ", cd1.genero );
printf( "El numero de canciones es: %i \n ", cd1.numCanciones);
printf( "El año del lanzamiento es: %i \n ", cd1.lanzamiento );
printf( "El precio es: %i \n ", cd1.precio );
return 0;
}
